# Contributing to ailoud

Thank you for contributing. This document covers commit conventions,
dependency policy, and GPG signing. Please read it in full before opening a
pull request.

---

## Conventional Commits

All commits in this repository follow the
[Conventional Commits 1.0](https://www.conventionalcommits.org/en/v1.0.0/) spec.

**Types:** `feat`, `fix`, `chore`, `docs`, `test`, `refactor`, `perf`, `ci`, `build`

**Rules:**

- Description in English, imperative mood ("add", not "added").
- Keep the subject line under 72 characters.
- Use `(scope)` sparingly -- only reuse a scope that already exists in the git
  log. Before adding a scope, run
  `git log --oneline | grep "type(" | head -20` to check what scopes are
  established. Prefer no scope over inventing a new one.
- No mentions of AI tools, agents, or assistants anywhere in commits: no
  `Co-Authored-By: Codex ...`, `Co-Authored-By: Claude ...` or similar lines,
  no "generated by", no "with the help of AI", no tool names in subject or body.
- Reference issues with `Relates #123` in the footer when applicable.

**Examples:**

```
feat: add the ls command
fix: offset segment timestamps after a split
chore: update vitest to 4.1
docs: document the doctor exit codes
```

---

## Changelog Entries

Every user-visible change gets a bullet under `## Development` in
[`CHANGES.md`](CHANGES.md), grouped under `### Added`, `### Changed`, or
`### Fixed`.

**Rules:**

- Keep each bullet to 25 words or fewer, counted as whitespace-separated
  tokens; a code span counts as one word whatever it contains. Wrap at the
  file's usual width, with continuation lines indented two spaces.
- The cap applies to `### Added`, `### Changed`, and `### Fixed` alike, and
  only to `## Development`. A section is frozen once released.
- English, and describe the change, not the code that made it.
- One bullet per change. Two changes that need separate wording are two
  bullets, never one longer one.

**Examples:**

```
### Changed

- `transcribe` retries a split part once before failing the whole
  recording, instead of aborting on the first provider error.

### Fixed

- `doctor` reports the configured STT model path even when the file is
  missing, instead of only saying "not found".
```

---

## Dependency Discipline

Add only packages that are genuinely required for the application to work.
Prefer the standard library and the existing dependency set before reaching
for a new library. Every new direct dependency in `package.json` must be
justified in the pull request description and must pass the license policy
below.

---

## Dependency License Policy

All direct third-party npm dependencies must carry a license compatible with
Apache 2.0 before being merged.

When adding a new library:

1. Run the `check-licenses` skill after editing any `package.json`.
2. Review the license table the skill displays and confirm each entry.
3. The skill updates `LICENSE` automatically.

Licenses that are NOT acceptable (prohibit commercial use or impose copyleft
incompatible with Apache 2.0):

- GPL-2.0 / GPL-3.0 / AGPL-3.0
- LGPL-2.1
- SSPL-1.0 / BSL-1.1
- Any Creative Commons -NC- variant
- Any license containing a "Commons Clause" addendum

Licenses that ARE acceptable include MIT, BSD-2-Clause, BSD-3-Clause,
ISC, Apache-2.0, 0BSD, and CC0-1.0.

---

## GPG Signing

Signing commits is strongly recommended. It lets maintainers verify that
commits genuinely come from you and have not been tampered with.

```bash
# Sign a single commit
git commit -S -m "feat: ..."

# Make signing the default for this repo
git config commit.gpgsign true
```

If you already have commits without signatures you can re-sign them before
opening a pull request:

```bash
# Find the last signed commit and rebase everything after it
LAST_SIGNED=$(git log --format="%G? %H" | awk '$1=="G"{print $2; exit}')
git rebase "$LAST_SIGNED" --exec "git commit --amend --no-edit -S"
```

---

## Branching

- `main` -- release branch. Direct commits are allowed only until the first
  release. After that, changes enter only via pull request.
- `develop` -- integration branch; feature branches accumulate here and merge
  to `main` at release time.
- `feature/<feature-name>` -- feature work, branched from `develop`.

A release commit prepares the version bump, then `develop` merges to `main`
via pull request.

Tags:

- `v<version>-rc.<n>` -- a release candidate, tagged on `develop`.
- `v<version>` -- a final release, tagged on `main` only.

Only release-candidate tags may be cut from `develop`. This is a convention
that maintainers follow by hand; no workflow enforces it yet.
