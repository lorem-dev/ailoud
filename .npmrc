# Only keys npm itself understands belong here: pnpm reads this file, but so
# does npm, and npm warns on every key it does not recognise. pnpm-only
# settings live in pnpm-workspace.yaml instead -- which is also the only place
# pnpm 11 still reads some of them from.
engine-strict=true
save-exact=true
